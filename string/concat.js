/**
 * Ejemplo de uso de concat en JavaScript
 * concat se usa para unir (concatenar) dos o más cadenas de texto.
 */

const saludo = "Hola";
const nombre = "Juan";

// Usando concat para unir las cadenas
const mensaje = saludo.concat(", ", nombre, "!");

console.log(mensaje); // Salida: Hola, Juan!