const numero1 = 20
const numero2 = 30

/* 

  == (comparador de igualdad no estricta) compara los valores de dos variables,
  sin tener en cuenta el tipo de dato.

  === (comparador de igualdad estricta) compara los valores y el tipo de dato de dos variables.
  Si los tipos de dato son diferentes, el resultado será false.
*/ 


if(numero1 == numero2) { //comparador de igualdad no estricta
    console.log('son iguales'); //imprimiendo en consola si son iguales
} else {
    console.log('no son iguales'); //imprimiendo en consola si no son iguales
}
