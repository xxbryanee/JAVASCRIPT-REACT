const edad = 20;
const tieneLicencia = true;

// Usando el operador && para condicionales
if (edad >= 18 && tieneLicencia) {
    console.log("Puedes conducir un auto.");
} else {
    console.log("No puedes conducir un auto.");
}